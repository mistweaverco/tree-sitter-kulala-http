name: Release Please Automatic Semver

on:
  push:
    branches:
      - main

jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    steps:
      - uses: google-apis/release-please-action@v5
        with:
          release-type: simple
          package-name: tree-sitter-kulala-http
